import { Component } from './component';

export default function HomePage() {
  return <Component />;
}

/** @type {import("next").Metadata} */
export const metadata = {
  title: 'Next.js Jukebox by candycode',
};
